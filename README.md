# Tropical Disease Monitor (Dify)
Pipeline semanal para gerar boletins de doenças tropicais com Dify + GitHub Actions.

## Rodar local
1. `python -m venv .venv && source .venv/bin/activate`
2. `pip install -r requirements.txt`
3. `cp .env.example .env` (preencha depois)
